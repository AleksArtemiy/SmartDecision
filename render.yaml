services:
  - type: web
    name: journal-system
    env: docker
    docker:
      dockerfile: Dockerfile
    envVars:
      - key: PHP_ENV
        value: production
    healthCheckPath: /index.php
    autoDeploy: true