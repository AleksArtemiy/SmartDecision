services:
  - type: web
    name: journal-system
    env: docker
    plan: free
    docker:
      dockerfile: Dockerfile
    envVars:
      - key: PHP_ENV
        value: production
    healthCheckPath: /health.php
    autoDeploy: true